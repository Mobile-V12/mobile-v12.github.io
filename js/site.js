(()=>{const search=document.getElementById('appSearch');const buttons=[...document.querySelectorAll('.filter-btn')];const apps=[...document.querySelectorAll('.app-profile')];const empty=document.getElementById('noResults');let active='all';const apply=()=>{const q=(search?.value||'').trim().toLowerCase();let shown=0;apps.forEach(app=>{const category=(app.dataset.category||'').split(/\s+/);const hay=(app.dataset.search||'').toLowerCase();const okCategory=active==='all'||category.includes(active);const okSearch=!q||hay.includes(q)||app.textContent.toLowerCase().includes(q);app.hidden=!(okCategory&&okSearch);if(!app.hidden)shown++});if(empty)empty.classList.toggle('show',shown===0)};buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');active=btn.dataset.filter||'all';apply()}));search?.addEventListener('input',apply);document.querySelectorAll('.store-carousel-wrap').forEach(wrap=>{const rail=wrap.querySelector('.store-carousel');if(!rail)return;const amount=()=>Math.max(260,rail.clientWidth*.72);wrap.querySelector('.prev')?.addEventListener('click',()=>rail.scrollBy({left:-amount(),behavior:'smooth'}));wrap.querySelector('.next')?.addEventListener('click',()=>rail.scrollBy({left:amount(),behavior:'smooth'}))});document.querySelectorAll('.app-icon-shell img').forEach(img=>img.addEventListener('error',()=>{img.style.display='none';const fallback=img.nextElementSibling;if(fallback)fallback.style.display='grid'}));})();